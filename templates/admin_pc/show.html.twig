{% extends 'base.html.twig' %}

{% block title %}Pc{% endblock %}

{% block body %}
    <h1>Pc</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pc.id }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ pc.brand }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ pc.prix }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ pc.model }}</td>
            </tr>
            <tr>
                <th>Specification</th>
                <td>{{ pc.specification }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_pc_index') }}">back to list</a>

    <a href="{{ path('app_admin_pc_edit', {'id': pc.id}) }}">edit</a>

    {{ include('admin_pc/_delete_form.html.twig') }}
{% endblock %}
